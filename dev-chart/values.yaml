# Default values for dev-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  imagePullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.1"



#image_adservice:
  #repository: gcr.io/team-3-a/adservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_cartservice:
 # repository: gcr.io/team-3-a/cartservice
 # pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_checkout:
  #repository: gcr.io/team-3-a/checkoutservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_currency:
  #repository: gcr.io/team-3-a/currencyservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_email:
  #repository: gcr.io/team-3-a/emailservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_frontend:
  #repository: gcr.io/team-3-a/frontend
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  #tag: ""

#image_load:
  #repository: gcr.io/team-3-a/loadgenerator
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_payment:
  #repository: gcr.io/team-3-a/paymentservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_product:
  #repository: gcr.io/team-3-a/productcatalogservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_recommendation:
  #repository: gcr.io/team-3-a/recommendationservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
 # tag: ""

#image_shippig:
  #repository: gcr.io/team-3-a/shippingservice
  #pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  #tag: ""